name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: npm test

    - name: Build Docker image
      run: docker build -t my-nest-app .

    - name: Save Docker image to tar
      run: docker save my-nest-app -o my-nest-app.tar

    # Étape 1 : Setup et vérification de la clé privée
    - name: Set up SSH key
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -t rsa 146.59.242.125 >> ~/.ssh/known_hosts

    # Étape 2 : Vérification de la clé privée et permissions
    - name: Debug SSH Key
      run: |
        echo "🔍 Vérification de la clé privée..."
        ls -lah ~/.ssh
        cat ~/.ssh/id_rsa | head -n 3

    # Étape 3 : Vérification du contenu du fichier known_hosts
    - name: Vérifier le fichier known_hosts
      run: |
        echo "🔍 Contenu du fichier known_hosts..."
        cat ~/.ssh/known_hosts

    # Étape 4 : Test de connexion SSH détaillée (mode verbeux)
    - name: Test SSH Connection
      run: |
        echo "🔍 Test de connexion SSH avec détails (mode verbeux)..."
        ssh -v -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no thomas@146.59.242.125 "echo '✅ Connexion SSH réussie !'"

    # Étape 5 : Vérifier le fichier authorized_keys sur le VPS (dans ton workflow GitHub)
    - name: Vérifier authorized_keys sur le serveur
      run: |
        echo "🔍 Vérification du fichier authorized_keys sur le serveur..."
        ssh -v -i ~/.ssh/id_rsa thomas@146.59.242.125 "cat ~/.ssh/authorized_keys"

    # Étape 6 : Copie du Docker image vers le VPS
    - name: Copy Docker image to VPS
      run: |
        echo "📦 Envoi du fichier my-nest-app.tar au serveur..."
        scp -v -i ~/.ssh/id_rsa my-nest-app.tar thomas@146.59.242.125:project-node/my-nest-app.tar

    # Étape 7 : Déploiement de l'image Docker sur le serveur
    - name: Deploy Docker image on VPS
      run: |
        echo "🚀 Déploiement de l'application sur le serveur..."
        ssh -v -i ~/.ssh/id_rsa thomas@146.59.242.125 << 'EOF'
          echo "🛠 Arrêt et suppression des anciens containers..."
          docker stop my-nest-app || true
          docker rm my-nest-app || true
          echo "📦 Chargement de l'image Docker..."
          docker load -i project-node/my-nest-app.tar
          echo "🚀 Lancement du container..."
          docker run -d --name my-nest-app -p 3000:3000 my-nest-app
          echo "✅ Déploiement terminé !"
        EOF
