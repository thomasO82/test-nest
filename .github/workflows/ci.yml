# Exemple complet d'attente du service 'api' avant de continuer

name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '23'

    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: npm test

    # Étape 1 : Setup et vérification de la clé privée
    - name: Set up SSH key
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -t rsa 146.59.242.125 >> ~/.ssh/known_hosts

    # Étape 2 : Test de connexion SSH
    - name: Test SSH Connection
      run: |
        echo "🔍 Test de connexion SSH avec détails (mode verbeux)..."
        ssh -v -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no thomas@146.59.242.125 "echo '✅ Connexion SSH réussie !'"

    # Étape 3 : Cloner le dépôt et déployer sur le VPS
    - name: Clone Git repository and deploy on VPS
      run: |
        echo "📦 Clonage du dépôt sur le serveur..."
        ssh -v -i ~/.ssh/id_rsa thomas@146.59.242.125 << 'EOF'
          cd project-node/test-nest
          git pull origin main
          echo "🛠 Arrêt et suppression des anciens containers..."
          docker-compose down || true
          echo "🚀 Lancement du container avec Docker Compose..."
          docker-compose up -d

          # Vérifier l'état des conteneurs
          docker-compose ps

          # Attente asynchrone pour vérifier si le service 'api' est prêt
          while ! docker-compose ps | grep -q 'api.*Up'; do
            echo "⏳ En attente du démarrage du service 'api'..."
            sleep 5  # Attendre 5 secondes entre chaque vérification
          done
          echo "✔ Le service 'api' est maintenant prêt."

          # Exécuter Prisma generate et migrate
          docker-compose exec api npx prisma generate
          docker-compose exec api npx prisma migrate deploy

          echo "✅ Déploiement terminé !"
        EOF
