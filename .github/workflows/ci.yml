name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '23'

    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: npm test

    # Étape 1 : Setup et vérification de la clé privée
    - name: Set up SSH key
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -t rsa 146.59.242.125 >> ~/.ssh/known_hosts

    # Étape 2 : Test de connexion SSH
    - name: Test SSH Connection
      run: |
        echo "🔍 Test de connexion SSH avec détails (mode verbeux)..."
        ssh -v -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no thomas@146.59.242.125 "echo '✅ Connexion SSH réussie !'"

    # Étape 3 : Cloner Git repository sur le VPS
    - name: Clone Git repository on VPS
      run: |
        echo "📦 Clonage du dépôt sur le serveur..."
        ssh -v -i ~/.ssh/id_rsa thomas@146.59.242.125 << 'EOF'
          cd project-node/test-nest
          git pull origin main
          echo "🛠 Arrêt et suppression des anciens containers..."
          docker-compose down -v || true
          echo "🚀 Lancement du container avec Docker Compose..."
          docker-compose up -d
          
          # Étape 4 : Vérification si la base de données est prête
          attempts=0
          max_attempts=20  # Limite le nombre de tentatives
          until docker-compose exec -T db sh -c "pg_isready -U postgres"; do
            attempts=$((attempts+1))
            if [ $attempts -ge $max_attempts ]; then
              echo "La base de données n'est pas prête après $attempts tentatives, sortie..."
              exit 1
            fi
            echo "En attente que la base de données soit prête... Tentative $attempts/$max_attempts"
            sleep 5
          done
        EOF

    - name: apply migrations
      run: |
        echo "📦 Clonage du dépôt sur le serveur..."
        ssh -v -i ~/.ssh/id_rsa thomas@146.59.242.125 << 'EOF'
          cd project-node/test-nest
          docker-compose -T exec api npx prisma generate
          docker-compos -T exec api npx prisma migrate dev --name init
        EOF
