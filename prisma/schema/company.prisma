model Company {
    id        Int      @id @default(autoincrement())
    name      String
    address   String
    city      String
    zip       String
    phone     String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
    employees Employee[]
}